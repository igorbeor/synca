# synca

Real-time collaborative whiteboard — Nx monorepo with an Angular web client and NestJS API + realtime services.

See `docs/superpowers/` for the design spec and the sprint-based implementation plans.
